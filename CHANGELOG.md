# Changelog

## v0.10.0 - 2026-09-05

前端全面统一与可拖动三栏（仅 index.html / styles.css / app.js，后端未动）：

- 三栏工作台支持左右拖动分隔条：Pointer Events + 指针捕获，导航栏 180–320px、订单栏 300–480px，宽度持久化到 `printops_layout`，双击重置，键盘左右箭头 ±16px 微调，≤1050px 抽屉模式自动禁用。
- 订单栏新增与导航栏对称的收起/展开按钮；左栏收起时对应分隔条自动隐藏。
- 设计令牌统一：新增间距 `--space-1..8`、圆角 `--radius-xs..pill`、字号阶梯（全部 ≤10px 字号提升到 ≥11px）、纸色/墨色阶梯；约 135 处硬编码色值与 53 处 rgba 归并到令牌；阴影收敛为印刷偏移硬阴影与柔和投影两种。
- 按钮收敛为 primary/secondary/ghost/chip 四类基类，禁用态统一为 opacity .45 + not-allowed（仅加载中 wait）；输入框样式收敛为 `.field` 基类并统一焦点环。
- 清理：删除 `.tip-panel/.tip-label/.profile-params` 等死选择器，合并重复的 `.field-row.required-missing`，修复 `catalog-list` 重复 id 导致折叠面板空白的问题，v0.9.0 补丁块并入主规则；字段更新高亮改为一次性渐隐动画。
- 交互修复：重置订单增加原生确认弹窗；推荐方案卡片键盘可达（role=button + Enter/Space）；聊天仅在贴近底部时自动滚动；输入框随内容 38–120px 自动长高；错误出口统一为"对话流→气泡、面板动作→toast"并区分成功/失败样式；移动端抽屉增加半透明遮罩，点击外部或 Esc 关闭；内联字段清空因后端不支持空 patch，提示文案改为引导对话修改。
- 文案统一：确认前"订单草稿"、确认后"交接单"；设置状态"接口已启用/接口异常"；"智能体在线"；移除开发腔"MVP"表述；用户消息角标改为"我"；对话句带句号、toast 不带；静态资源缓存参数升级。
- 收起语义修复：三栏 grid 子元素改为显式列放置，修复「收起导航栏/订单栏后对话主区消失」的自动补位错位（存量缺陷在加入右栏收起后暴露）；收起后原位置保留 20px 竖排边缘把手（导航栏/订单栏），点击或键盘 Enter 即展开，把手仅桌面模式出现，拖拽与 390px 抽屉模式回归通过。
- 视觉验收修复：`.file-state` 的 `display:block` 覆盖 `hidden` 属性导致订单栏出现无标签空框（存量 bug）；订单历史在未选方案时误标「已选方案」；空字段占位显示字段名自身；≤420px 顶栏溢出（隐藏品牌标记与英文标语、收紧间距）。
- docs/ROADMAP.md 新增"系统优化路线图"：安全（本地鉴权、Key 明文落盘、静态目录暴露、SSRF 面）、可靠性（LLM 全局锁、阻塞无流式、重试策略、SQLite WAL、会话增长）、工程化（模块拆分收尾、双轨 NLU 一致性评测、消息长度上限、打包交付）。

## v0.9.1 - 2026-09-05

- 评测集从 5 例扩展到 111 例脱敏用例，按基础/否定/修改/歧义/多产品/尺寸格式/数量表达分组输出字段准确率；完成率口径改为"可完成用例"（14 例补全轮次后基座完整），质量门槛提高为字段准确率 ≥95% 且可完成用例完整率 ≥80%。
- 尺寸识别支持个位数维度（如 `5×5cm` 小标签），分隔符与数量判定互不干扰。
- 包装盒新增内尺寸/外尺寸语义（`productSpecs.boxSizeInner/boxSizeOuter`），订单草稿与多产品编辑同步展示；`validate_order` 在三维尺寸未标注内/外语义时给出提示（糊盒刀模以内尺寸为准）。
- 感知修复五类真实缺陷：否定窗口不再跨越标点（"不要烫金，改哑膜"正确生效）；后续修改轮（"改成可移胶"）沿用当前品类提示补齐规格上下文；"500 包装盒"的"包"不再被误判为数量单位；多产品数量归属从中心点启发式改为"数量就近跟随其后产品"规则并以中点分段保留前置限定词（"天地盖包装盒"）；"不需要内淋膜"与"智能卡含芯片"识别补齐。
- 交期识别确认无回归后评测全绿：111 例字段准确率 100%，可完成用例完整率 100%，109 个单元测试通过。

## v0.9.0 - 2026-09-05

- 规则感知引入真实置信度分级：显式标签/带单位数值 0.9 以上，裸数字、氛围词与启发式归属低于 0.75 确认阈值；生产字段低置信度时阻止生成交接单，预算/用途等偏好字段只提示不拦截；用户重述相同数量也能刷新字段确认状态。
- `estimate_price` 改为消费带版本的示例价格参数表（品类基数、数量阶跃、工艺系数、版费地板），响应携带 `pricingModelVersion`，不再输出无依据的线性估价。
- 工艺推荐引入合版/专版语义：经济/平衡方案对应合版拼单，质感方案对应专版开机，说明批次色差与颜色可控性；方案卡片新增“印刷方式”。
- `validate_order` 新增品类起印量提示；`prepare_handoff` 交接文本新增“开数参考”（大度/正度整裁数与纸张利用率，标注为参考值）。
- 供应商字段映射支持品类分层（`"*"` 通用映射 + 按品类覆盖，支持 `productSpecs.<key>` 路径），为真实平台接入固化结构。
- 代码结构拆分：`order_model.py`（数据契约与规范化）、`nlu.py`（规则感知）、`tools.py`（工具注册表）；会话状态引入 `schemaVersion`，迁移集中到 `normalize_state`。
- LLM 规划调用改为发送压缩订单摘要 + 缺失字段 + 工作流阶段；工具 schema 为每个参数补充中文描述，降低 token 消耗与参数误猜。
- 前端：订单草稿基础字段支持 inline 编辑（多产品模式保持只读，清空仍走对话否定流）；进度条改为字段信息度，不再使用按阶段虚构的百分比；左栏产品索引/工具箱默认折叠，运行状态并入决策面板；设置保存降为次级按钮、工具箱代码统一、正文与字段可读性提升。
- 新增 MIT `LICENSE`、GitHub Actions CI（编译、单测、评测、敏感信息扫描、空白检查）与 `tools/secret_scan.py`；知识版本 `2026.09.05`。

## v0.8.2 - 2026-09-04

- 重写 README，统一本地默认/验收端口为 `4174`，补充跨平台启动、端口排障、LLM 配置、安全和 GitHub 发布说明。
- 新增 `dimensions.finishedSize`、`expandedSize`、`dieCutSize` 和 `packageSize`，保留旧 `size` 字段兼容。
- 识别“成品尺寸/展开尺寸/刀模尺寸”等标注，包装盒与手提袋的长宽高不再误标为平面成品尺寸。
- 旧会话、旧 `fieldMeta` 和模型误写入的 `productSpecs` 尺寸别名会在加载或更新时自动迁移。
- 订单草稿、多产品项编辑和导出内容展示明确的尺寸语义；新增尺寸隔离与迁移回归测试。

## v0.8.1 - 2026-09-04

- 为品类参数、尺寸语义、风险边界和供应商能力档案增加可追溯的知识版本与来源清单。
- `/api/products` 返回知识清单、来源范围、成品/展开/刀模/包装三维尺寸定义及人工确认策略。
- 订单校验、工艺估算、供应商能力匹配和会话响应带回知识版本，便于后续更新规则后定位依据。
- 新增知识版本 API 与 Agent 契约测试，确保知识更新不会静默改变订单解释边界。

## v0.8.0 - 2026-09-04

- 询价请求持久化到 SQLite 会话状态，使用标准化订单、目标平台和产品项 `itemId` 生成稳定幂等键；相同请求重试会复用原请求。
- 增加 `POST /api/quote/status` 和 `POST /api/quote/cancel`，支持询价状态回查、取消及重复取消保护；订单、文件或平台变化会将活动请求标记为 `stale`。
- 询价前阻止未完成订单或未选择工艺方案，避免生成不可交付的询价；人工确认交接单后，待确认询价转为 `confirmed`，仍不会向供应商发起网络请求。
- 订单草稿增加询价状态区和请求操作；新增 Agent 与 API 生命周期测试，Python 单元测试共 68 项。

## v0.7.4 - 2026-09-04

- 交接单状态持久化，明确区分“草稿已生成”和“人工已确认”；新增 `/api/confirm`，确认动作不会向供应商提交。
- 订单字段或目标平台变化会自动撤销旧交接单和人工确认，避免继续使用过期方案。
- 多产品 PDF 预检必须指定产品项，检查结果与稳定 `itemId` 绑定；单产品继续兼容原有顶层文件字段。
- 抽出无网络副作用的 API 路由分发层，补充会话、`itemIndex`、文件归属、确认和错误路由契约测试；Python 单元测试共 63 项，订单评测保持 100% 字段准确率和完整率。

## v0.7.3 - 2026-09-04

- 为每个多产品项补齐稳定 `itemId`、独立状态和逐项校验结果，返回字段缺失、品类参数缺失与信息度。
- “处理第 N 项”后的自然语言更新只写入对应产品项；`/api/chat` 支持 `itemIndex`，并记录逐项字段来源和冲突。
- 多产品面板显示每项完成状态、信息度和下一项待补字段；保留高风险工具阻断，未将此版本描述为完整多行下单。
- 新增 [docs/ROADMAP.md](docs/ROADMAP.md)，明确 v0.8 多行订单闭环和后续质量门槛；Python 单元测试共 54 项，订单评测保持 100% 字段准确率和完整率。

## v0.7.2 - 2026-09-04

- 订单草稿新增独立产品项面板，展示每项关键数量、尺寸、材料、颜色和后道信息。
- 支持从产品项切入对话并持久化当前 `activeItemIndex`，顶部概览改为显示多项摘要，避免把单项数量误当成总订单数量。
- 多产品解析会把明确的公共参数复制到缺少该字段的产品项，产品专属参数继续保持独立。
- 浏览器实测多产品样例可显示 2 个产品项并完成逐项切换；Python 单元测试共 50 项，订单评测保持 100% 字段准确率和完整率。

## v0.7.1 - 2026-09-04

- 多产品自然语言需求会拆分为 `productTypes` 与 `items`，并按产品提及位置归属数量。
- 未拆分的多产品订单统一进入品类澄清阶段，不允许合并生成工艺推荐、价格估算、供应商询价或交接单。
- 询价、估价和交接工具返回结构化 `blocked` 原因，避免 UI 或模型把空结果误解为成功。
- 新增多产品高风险工具边界测试；Python 单元测试共 50 项，订单评测保持 100% 字段准确率和完整率。

## v0.7.0 - 2026-09-04

- 新增无网络副作用的 `SupplierAdapter` 协议和平台适配器注册表，标准订单可映射为供应商字段而不修改 Agent 主流程。
- 新增 `request_supplier_quote` 工具和“询价”对话意图：只生成带 `requestId`、能力状态和映射订单的待确认请求，不伪造价格、不直接提交供应商。
- 目标平台能力不支持时，询价请求明确阻断；支持或待确认时统一进入 `quote` 工作流阶段。
- UI 工具箱、工具 Schema、架构文档和测试同步更新；单元测试共 48 项，订单评测保持 100% 字段准确率和完整率。

## v0.6.2 - 2026-09-04

- LLM 规划器对连接失败、超时和 408/429/5xx 响应执行一次有上限的短重试，避免瞬时网络抖动直接触发规则回退。
- 重试成功后清除旧错误状态；解析错误和不可重试的业务错误不重复请求。
- 新增瞬时连接失败恢复测试；单元测试共 47 项，订单回归评测保持 100% 字段准确率和完整率。

## v0.6.1 - 2026-09-04

- 预检支持将订单平面尺寸与 PDF 的 TrimBox/MediaBox 比对，区分匹配、尺寸不一致和缺少页面框三种结果。
- 尺寸不一致只产生可解释警告，并提示确认成品尺寸与展开尺寸，避免把包装三维尺寸误当平面尺寸。
- `/api/preflight` 和公开工具契约支持 `expectedSize`；保持原稿不上传和人工印前确认边界。
- 单元测试维持 46 项全部通过，订单回归评测字段准确率和完整率维持 100%。

## v0.6.0 - 2026-09-04

- PDF 预检增加浏览器本地元数据扫描：页面框、PDF 版本、文件结束标记、颜色空间、字体嵌入标记、图片对象、透明度和叠印线索。
- `/api/preflight` 与 `preflight_file` 接收结构化 `inspection` 元数据；服务端对缺少 TrimBox/BleedBox、RGB、专色和透明度等情况给出可解释的人工确认提示。
- 明确预检边界：不上传原稿，不将轻量线索描述为专业印前放行结论。
- 新增元数据预检回归测试；单元测试共 46 项，既有 5 个订单评测场景保持 100% 字段准确率和完整率。

## v0.5.1 - 2026-09-04

- 修正数量单位识别：保留用户明确提供的张、份、本、册、个、块等单位，不再把所有品类统一写成“份”。
- 新增 `quantityValue` 与 `quantityUnit` 结构化字段，兼容旧版 `quantity` 展示文本并自动迁移旧会话。
- LLM 数量补丁经过同一套规范化，按品类生成缺数量快捷选项。
- 新增确定性回归评测脚本，覆盖尺寸、品类专属参数、多轮修改和结构品类；当前 5 个场景字段准确率与基础订单完整率均为 100%。
- 单元测试从 43 项增加到 45 项。

## v0.5.0 - 2026-09-04

- 新增 `match_supplier_capability` 能力匹配工具，按品类、材料、工艺、尺寸和交期返回支持、待确认和不支持项。
- 平面尺寸支持 A3/A4/A5/B4/B5、带单位自定义尺寸和 A3+ 档案范围判断；包装长宽高会保守转为结构/刀模待确认，不误报为平面尺寸不支持。
- 所有订单响应新增当前目标平台能力摘要；生成订单前阻止已明确不支持的材料、工艺或品类，并保留供应商人工确认边界。
- `/api/tools` 新增供应商能力工具契约，方便后续接入平台适配器和实时询价接口。
- 新增供应商能力匹配和尺寸边界测试，Python 单元测试共 43 项全部通过。

## v0.4.0 - 2026-09-04

- 新增轻量 Agent Runtime 运行记录：每次对话、选方案、预检和生成草稿都有独立 `runId`、步骤、状态和耗时。
- 新增工作流阶段：需求收集、品类澄清、方案选择、订单确认；接口同时返回阶段标签和下一步决策依据。
- 新增订单字段来源与置信度：区分用户输入、规则识别、模型推断和方案带入，低置信度字段在生成草稿前必须人工确认。
- 新增字段修改冲突记录，格式化差异不会被误判为冲突，用户确认相同值时会提升为用户来源。
- 工具注册表新增输入/输出契约，`/api/tools` 和模型上下文可读取统一结构。
- 订单记忆兼容保存最近运行记录、字段元数据和冲突信息。
- 新增 5 项 Runtime、来源和低置信度确认测试，Python 单元测试共 41 项全部通过。

## v0.3.0 - 2026-09-04

- 新增订单历史列表，自动保留最近 8 个有实际内容的订单。
- 支持多订单切换，恢复后端会话并在界面上高亮当前订单。
- 重置当前订单不会删除本地历史，方便演示时快速往返不同订单。
- 增强 PDF 上传体验：内置预览外增加页数、加密、大小和常见命名风险提示。
- 新增订单参数矛盾校验，优先识别画册页数与骑马钉装订风险。
- 订单区新增“风险与缺失”面板，统一展示必填缺失和结构化校验风险。
- 新增常用规格记忆，可保存、套用和删除最近使用的工艺组合，最多保留 8 组。
- 平台注册表新增供应商能力档案，订单交接单会标出已支持项和需人工确认项。
- 修复旧版 Safari 对结构化数据克隆的兼容问题，并支持清除可选品类参数。

## v0.2.0 - 2026-09-03

- 升级订单工作台：强化订单概览、产品规格、生产工艺和推荐方案的分区呈现。
- 新增高对比模式，改善浅色界面下的可读性和演示效果。
- 新增订单草稿导出为 JSON、CSV 和 Markdown，便于线下交接与归档。
- 产品参数表单补充必填状态与输入提示，减少无效提交。
- 保留 v0.1.0 的会话记忆、规则 Agent、可选模型接口与多平台适配能力。

## v0.1.0 - 2026-09-03

- 首个可验收 MVP 版本。
- 支持自然语言收集印刷订单、会话记忆、品类参数、工艺推荐和订单草稿。
- 支持规则 Agent 与可选的 OpenAI 兼容模型接口。
- 支持多平台注册表、白名单工具调用、请求追踪和安全的本地 Key 保存。
- 完成 VS Code 工作区式三栏布局、可折叠导航、长对话滚动和移动端适配。
